# Public Portal Deploy (Minimal)

This folder is safe for a public repository because it only contains built frontend assets.

Generated by:
- npm run export:public-portal:root

Render static site settings:
- Root Directory: leave empty
- Build Command: leave empty (or run npm run export:public-portal:root if required)
- Publish Directory: .
